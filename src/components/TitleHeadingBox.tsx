import { styled } from '../../stitches.config';

const HeadingBox = styled('div', {
  fontFamily: '$heading',
  fontSize: '35px',
  color: '$purple',
  marginBottom: '2rem',
});

export default HeadingBox;
